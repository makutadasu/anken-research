import { defineConfig } from 'vite';

export default defineConfig({
    // GitHub Pagesでホスティングするためのベースパス設定
    // リポジトリ名が 'anken-research' の場合、ベースパスは '/anken-research/' になります。
    base: '/anken-research/',
});
